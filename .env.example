# Optional override for the SmartBill V1 base URL.
# Credentials are never read from env in this MVP — the browser sends them
# per request to /api/smartbill/* (BFF). Do not put tokens here or in git.
SMARTBILL_BASE_URL=https://ws.smartbill.ro/SBORO/api
